name: my_app
description: A new Flutter project.

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  image_picker: ^1.1.2

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
